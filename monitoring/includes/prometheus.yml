volumes:
  prometheus:

services:
  prometheus:
    container_name: prometheus
    image: prom/prometheus:latest
    command:
      - --config.file=/etc/prometheus/prometheus.yml
    deploy:
      resources:
        limits:
          memory: 512M
    healthcheck:
      test: [ "CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:9090/-/healthy" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    volumes:
      - ./files/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus:/prometheus # This data isn't critical long-term, so a named volume is fine
      - /var/run/docker.sock:/var/run/docker.sock:ro # Used for Docker service discovery
    restart: unless-stopped
    labels:
      traefik.enable: true
      traefik.http.routers.prometheus.rule: Host(`prometheus.sagittarius.starsystem.dev`)
      traefik.http.routers.prometheus.entrypoints: websecure
      traefik.http.routers.prometheus.middlewares: intranet-only@docker,basic-auth@docker
      traefik.http.services.prometheus.loadbalancer.server.port: 9090